// src/components/SignatureOutletsSidebar.jsx

import React from 'react';

function SignatureOutletsSidebar() {
  return (
    // We reuse the 'about-sidebar' class from our existing CSS
    <div className="about-sidebar">
      <h5 className="sidebar-title">Signature Outlets</h5>
      {/* No navigation links are needed here as per the image */}
    </div>
  );
}

export default SignatureOutletsSidebar;